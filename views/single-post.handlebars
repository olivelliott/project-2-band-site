
  <div class="flex-row justify-space-between pb-2 mt-5">
    <h2>
      <a href="/posts/{{post.id}}">
        <div class="col-8">{{post.forum-title}}</div>
      </a>
    </h2>
    <div class="col-3 mx-4">Posted by {{user.username}} on {{format_date post.created_at}}
    </div>
  </div>  
  <p class="card mx-5 px-3 py-2 forum-text">
   {{post.content}}
  </p>

<input type="hidden" name="post-id" value="{{post.id}}"/>
{{!-- {{#if loggedIn}} --}}

<div class="bg-light">
{{comment.comment_text}}
</div>

  <form class="card-body mx-4" id="new-comment-form">
    
    <p class="form-label">Comment:</p>
    <textarea class="form-input form-control card" id="comment-body" name="comment-body"></textarea>
    <br>
    <button type="submit" class="btn btn-primary">Submit:</button>
  </form>
{{> comments post.comments}}

{{!-- {{/if}} --}}
<script src="/js/comment.js"></script>


{{!-- {{> forum-info post}}

<div>{{post.content}}</div>


{{#if loggedIn}}


<form class="comment-form">
  <div>
    <textarea name="card-body comment-body"></textarea>
  </div>

  <div>
    <button type="submit">Add comment</button>
  </div>
</form>

{{/if}}

{{> comments post.comments}}


{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
{{/if}} 

<h1>hello</h1> --}}


