const router = require("express").Router();

// GET all users
// ! exclude the password

// GET one user by id

// CREATE a new user
// ! username, email, password, facebook & instagram (if provided)

// LOGIN route

// LOGOUT route

// UPDATE a user by id

// DELETE a user

module.exports = router;
